repos:
  - repo: local
    hooks:
      - id: format-blocks
        name: Format scriptBlocks.json
        entry: python formatBlocks.py
        language: python
        files: ^data/blocks/.*\.json$
        pass_filenames: false
        always_run: true