repos:
  - repo: local
    hooks:
      - id: format-blocks
        name: Format scriptBlocks.yaml
        entry: python formatBlocks.py
        language: python
        files: ^data/blocks/.*\.yaml$
        pass_filenames: false
        always_run: true
        additional_dependencies: [pyyaml]