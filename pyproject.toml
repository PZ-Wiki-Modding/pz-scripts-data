[project]
name = "pz-scripts-data"
description = "Dataset for the Scripts of Project Zomboid."
requires-python = ">=3.12.3"
version = "1.0.1"
license-files = ["LICENSE"]
authors = [
  { name = "SimKDT" },
]
dependencies = [
    "pyyaml",
    "jsonschema",
]